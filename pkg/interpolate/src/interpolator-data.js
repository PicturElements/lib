const interpolatorData = {
	reservedKeys: {
		at: true,
		easing: true
	},
	reservedKeysList: null
};

interpolatorData.reservedKeysList = Object.keys(interpolatorData.reservedKeys);

export default interpolatorData;
